angular.module('app.core').factory('mockData', function () {
    return {
        get: function (url, option) {
            switch (url) {

                default:
                    break;
            }
        }
    };

});
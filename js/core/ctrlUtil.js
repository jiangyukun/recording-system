/**
 * jiangyukun on 16/5/27.
 */
+function (window) {
    'use strict';

    var moment = window.moment;
    var angular = window.angular;
    angular.module('app.core').factory('util', function ($rootScope, constants, settings, toastr, $q, ngDialog, $uibModal) {
        return {}
    });
}(window);

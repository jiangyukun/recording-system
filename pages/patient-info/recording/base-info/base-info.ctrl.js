+function (window) {
    'use strict';

    var angular = window.angular;
    angular.module('app').controller('RecordingBaseInfoController', function ($stateParams) {
        var vm = this;


        // - - - - - - - - - - - - - - - - - - - - - - - - - -

        console.log($stateParams);
    });
}(window);
